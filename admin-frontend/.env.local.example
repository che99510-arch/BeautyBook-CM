# API Configuration (unused in mock mode)
# NEXT_PUBLIC_API_URL=http://localhost:8000/api/admin

# For production
# NEXT_PUBLIC_API_URL=https://api.beautybook.cm/api/admin

# The service currently returns hardcoded mock data and does not
# make any network requests. Remove the comments above when you
# integrate a real backend.
